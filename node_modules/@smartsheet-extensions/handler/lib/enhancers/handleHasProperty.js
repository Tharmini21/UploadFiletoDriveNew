"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.handleHasProperty = void 0;
const BadRequestError_1 = require("../errors/BadRequestError");
/**
 * This handler will throw `BadRequestError` if a given property does not
 * exist on the payload.
 *
 * @example
 * ```js
 * return createExtensibleHandler(handleHasProperty('abc'))
 * // Pass
 * { "abc": 'Hello, World!' };
 * // Fail
 * { "notAbc": 'Hello, World!' };
 * ```
 */
exports.handleHasProperty = (property) => create => () => {
    const handler = create();
    return (payload, callback) => {
        if (payload[property] === undefined || payload[property] === null) {
            throw new BadRequestError_1.BadRequestError(`Request body must contain \`${property}\` property.`);
        }
        handler(payload, callback);
    };
};
//# sourceMappingURL=handleHasProperty.js.map