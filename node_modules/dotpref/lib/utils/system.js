"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getSystemConfigPath = void 0;
const os_1 = require("os");
const path_1 = require("path");
const getMacOSConfigPath = (name) => {
    return path_1.join(os_1.homedir(), 'Library', 'Preferences', name);
};
const getWindowsConfigPath = (name) => {
    return path_1.join(os_1.homedir(), 'AppData', 'Roaming', name, 'Config');
};
const getLinuxConfigPath = (name, path) => {
    if (path) {
        return path_1.join(path, name);
    }
    return path_1.join(os_1.homedir(), '.config', name);
};
exports.getSystemConfigPath = (name, platform, xdgConfigHome) => {
    if (platform === 'darwin') {
        return getMacOSConfigPath(name);
    }
    if (platform === 'win32') {
        return getWindowsConfigPath(name);
    }
    return getLinuxConfigPath(name, xdgConfigHome);
};
