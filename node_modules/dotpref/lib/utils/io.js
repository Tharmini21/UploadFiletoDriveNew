"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.readFromDisk = exports.writeToDisk = void 0;
const fs_1 = require("fs");
const path_1 = require("path");
const write_file_atomic_1 = require("write-file-atomic");
exports.writeToDisk = (dirPath, filename, payload) => {
    const filePath = path_1.join(dirPath, filename);
    fs_1.mkdirSync(dirPath, { recursive: true, mode: parseInt('0700', 8) });
    write_file_atomic_1.sync(filePath, payload, { mode: parseInt('0600', 8) });
};
exports.readFromDisk = (dirPath, filename) => {
    const filePath = path_1.join(dirPath, filename);
    return fs_1.readFileSync(filePath, 'utf8');
};
