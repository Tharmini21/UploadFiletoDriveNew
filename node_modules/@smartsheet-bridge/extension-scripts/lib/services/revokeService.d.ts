export interface CreateRevokeService {
    host: string;
    auth: string;
    options: {
        force: boolean;
        specFile: string;
        name?: string;
    };
}
export declare const createRevokeService: ({ host, auth, options: { force, specFile, name }, }: CreateRevokeService) => () => Promise<void>;
export declare type CreateRevokeServiceFn = typeof createRevokeService;
//# sourceMappingURL=revokeService.d.ts.map