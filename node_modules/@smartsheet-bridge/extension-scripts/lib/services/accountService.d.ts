import { AnyState } from 'dotpref';
import { AuthOptions } from '../options';
export interface Preferences extends AnyState {
    account: {
        [alias: string]: AuthOptions;
    };
}
export declare const createAccountService: () => {
    getAccount: (alias: string) => AuthOptions;
    saveAccount: (alias: string, { url, key }: AuthOptions) => void;
    removeAccount: (alias: string) => AuthOptions;
    listAccounts: () => {
        url: string;
        key: string;
        alias: string;
    }[];
};
export declare type CreateAccountServiceFn = typeof createAccountService;
//# sourceMappingURL=accountService.d.ts.map