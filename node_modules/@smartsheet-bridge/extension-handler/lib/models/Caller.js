"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Caller = void 0;
class Caller {
    constructor({ invoker, provider, callTime, msgid, installUUID, pluginUUID, callToken, revision, instanceID, } = {}) {
        if (invoker)
            this.invoker = invoker;
        if (provider)
            this.provider = provider;
        if (callTime)
            this.callTime = callTime;
        if (msgid)
            this.msgid = msgid;
        if (installUUID)
            this.installUUID = installUUID;
        if (pluginUUID)
            this.pluginUUID = pluginUUID;
        if (callToken)
            this.callToken = callToken;
        if (revision)
            this.revision = revision;
        if (instanceID)
            this.instanceID = instanceID;
    }
    static create(props = {}) {
        return new Caller(props);
    }
}
exports.Caller = Caller;
//# sourceMappingURL=Caller.js.map