"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.TextChannelMessage = void 0;
class TextChannelMessage {
    constructor({ uid, text } = {}) {
        if (uid)
            this.setUid(uid);
        if (text)
            this.setText(text);
    }
    static create(props = {}) {
        return new TextChannelMessage(props);
    }
    /**
     * Sets the unique identifier for the channel message.
     * @param uid the unique identifier for the channel message.
     */
    setUid(uid) {
        this.uid = uid;
    }
    /**
     * Sets the text for the channel message.
     * @param text the text for the channel message.
     */
    setText(text) {
        this.text = text;
    }
    toSerializableObject() {
        // kinda pointless with this class but each ChannelMessage needs to implement this interface
        return {
            uid: this.uid,
            text: this.text,
        };
    }
}
exports.TextChannelMessage = TextChannelMessage;
//# sourceMappingURL=TextChannelMessage.js.map