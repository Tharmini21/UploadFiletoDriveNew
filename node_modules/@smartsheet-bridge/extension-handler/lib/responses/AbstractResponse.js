"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.AbstractResponse = void 0;
const handler_1 = require("@smartsheet-extensions/handler");
class AbstractResponse {
    constructor(status = handler_1.ExtensionStatus.SUCCESS) {
        /**
         * The response status.
         */
        this.status = handler_1.ExtensionStatus.SUCCESS;
        this.setStatus(status);
    }
    /**
     * Sets the status on the response.
     * @param status the status to set.
     */
    setStatus(status) {
        if (typeof status !== 'number' ||
            status < 0 ||
            status > 6 ||
            Number.isNaN(status) ||
            !Number.isFinite(status) ||
            !Number.isInteger(status)) {
            throw new handler_1.InternalError(`\`status\` must be of type \`ExtensionStatus\` or \`number\` between 0 and 6. Recieved \`${typeof status}\`.`);
        }
        this.status = status;
    }
}
exports.AbstractResponse = AbstractResponse;
//# sourceMappingURL=AbstractResponse.js.map