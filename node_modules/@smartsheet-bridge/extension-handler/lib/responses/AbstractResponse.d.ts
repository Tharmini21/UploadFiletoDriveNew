import { ExtensionResponse, ExtensionStatus } from '@smartsheet-extensions/handler';
export declare abstract class AbstractResponse implements ExtensionResponse {
    /**
     * The response status.
     */
    status: ExtensionStatus;
    constructor(status?: ExtensionStatus);
    /**
     * Sets the status on the response.
     * @param status the status to set.
     */
    setStatus(status: ExtensionStatus): void;
}
//# sourceMappingURL=AbstractResponse.d.ts.map